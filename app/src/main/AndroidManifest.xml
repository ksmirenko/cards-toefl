<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.ksmirenko.toeflcards" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:fullBackupContent="@xml/backupscheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".layout.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.ksmirenko.toeflcards.layout.CardActivity"
            android:parentActivityName=".layout.MainActivity">
            <!--<intent-filter>
                <action android:name="com.ksmirenko.toeflcards.layout.CardActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>-->
        </activity>
        <activity
            android:name="com.ksmirenko.toeflcards.layout.DictionaryActivity"
            android:parentActivityName=".layout.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH"/>
            </intent-filter>
            <meta-data android:name="android.app.searchable"
                android:resource="@xml/searchable_dict"/>
        </activity>
    </application>

</manifest>
